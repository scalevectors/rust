
// Create a new rust project
cargo new hello_rust
cargo build
cargo run
cargo check // only check the code is compilable or not, it doesn't create exe filecd 
cargo build --release
cargo update // update the crates

// Important details
variables are immutable by default
String is a UTF8 encoded bit of text
The & indicates that this argument is a reference
